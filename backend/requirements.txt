# Pinned to the exact versions verified in the working environment.
# Bump deliberately and re-run the test suite; do not use floating ranges.
Django==6.0.8
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
# Google Sign-In; the [socialaccount] extra pulls in requests/PyJWT/etc.
django-allauth==65.19.1
requests==2.34.2
# Transactional email (OTP codes) via the Resend HTTP API (kyc/email.py).
resend==2.36.0
PyJWT==2.13.0
cryptography==50.0.0
oauthlib==3.3.1
cffi==2.1.1
pycparser==3.0
gunicorn==26.0.0
whitenoise==6.12.0
# PostgreSQL driver: psycopg 3; the [binary] extra ships its own libpq.
psycopg[binary]==3.3.4
dj-database-url==3.1.2
python-dotenv==1.2.2
python-json-logger==3.3.0
# Phone validation/normalisation (libphonenumber port), mirroring the SPA.
phonenumbers==9.0.37
# Community-maintained disposable/burner email blocklist. Bump to pick up new
# providers.
disposable-email-domains==0.0.237
# Caching/throttling uses Django's database cache backend (Postgres-backed).
