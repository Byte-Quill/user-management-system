# Pinned to the exact versions verified in the working environment.
# Bump deliberately and re-run the test suite; do not use floating ranges.
Django==6.0.8
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
# Google Sign-In (OIDC ID-token verification + user provisioning). The
# [socialaccount] extra pulls in requests/PyJWT/cryptography/oauthlib.
django-allauth==65.19.1
requests==2.34.2
# Transactional email (OTP codes) via the Resend HTTP API; wrapped by the
# Django backend in kyc/email.py.
resend==2.36.0
PyJWT==2.13.0
cryptography==50.0.0
oauthlib==3.3.1
cffi==2.1.1
pycparser==3.0
# CSP is provided by Django 6 itself (SECURE_CSP + ContentSecurityPolicyMiddleware).
gunicorn==26.0.0
whitenoise==6.12.0
# PostgreSQL driver: psycopg 3 (the modern, actively maintained successor to
# psycopg2). Django 6 prefers it when both are importable. The [binary] extra
# ships its own libpq, so no system build deps are needed.
psycopg[binary]==3.3.4
dj-database-url==3.1.2
python-dotenv==1.2.2
python-json-logger==3.3.0
# Caching/throttling uses Django's database cache backend (Postgres-backed);
# no Redis service or client is required.
